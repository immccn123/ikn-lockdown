<Page
    x:Class="Lockdown.Panel.Pages.FileListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lockdown.Panel.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{x:Bind ViewModel}"
    mc:Ignorable="d">

    <ScrollViewer Padding="12">
        <Grid DragOver="{x:Bind Grid_DragOver}" Drop="{x:Bind ViewModel.DropAsync}" AllowDrop="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- CommandBar -->
            <CommandBar DefaultLabelPosition="Right" Grid.Row="0">
                <AppBarButton
                    Icon="Add"
                    Click="{x:Bind AddFileAsync}"
                    Label="添加" />
                <AppBarButton
                    Icon="Remove"
                    Label="移除">
                    <AppBarButton.Flyout>
                        <Flyout>
                            <StackPanel>
                                <TextBlock Style="{ThemeResource BaseTextBlockStyle}" Text="是否移除选中文件？" Margin="0,0,0,12" />
                                <Button Command="{Binding DeleteSelectedCommand}" Content="确定" />
                            </StackPanel>
                        </Flyout>
                    </AppBarButton.Flyout>
                </AppBarButton>
            </CommandBar>

            <!-- ListView -->
            <ListView
                Grid.Row="1"
                ItemsSource="{Binding FileItems}"
                SelectionMode="{Binding SelectionMode, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:FileItemViewModel">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <CheckBox
                                IsChecked="{x:Bind IsSelected, Mode=TwoWay}"
                                VerticalAlignment="Center">
                                <StackPanel>
                                    <TextBlock
                                        Grid.Column="1"
                                        Text="{x:Bind FileName}"
                                        VerticalAlignment="Center"
                                        Margin="8,0,0,0" />
                                </StackPanel>
                            </CheckBox>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </ScrollViewer>
</Page>
